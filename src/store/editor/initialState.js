import * as size from "config/size";

export default {
	editorPanelShow: true,
	previewParams: {

		// id,
		// index,
		// name,
		// position,
		// size,
		// lockOnTarget,
		// isTargetRenderIn,
		// isTargetRenderOut,
		// componentProps,
	},
	fieldSize: size.F_HD,
};