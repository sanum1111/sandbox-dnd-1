import * as size from "./size";
import screen from "./screen";

export default {
	[size.F_HD]: [
		screen[size.F_HD][0] / 20,
		screen[size.F_HD][0] / 20,
	],
	[size.HD]: [
		screen[size.HD][0] / 20,
		screen[size.HD][0] / 20,
	]
};